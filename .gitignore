build/
build-*/
compile_commands.json
*.o
*.a

# Local working notes -- never committed.
/NOTES/

# Runtime artifacts from manually running shuzagram_server in this
# checkout (RSA private key, logs) -- never committed, generated fresh per
# deployment.
*.pem
*.log
